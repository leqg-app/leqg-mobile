{
  "hooks": {
    "after:bump": "yarn react-native bump-version --skip-semver-for all"
  },
  "git": {
    "commitMessage": "${version}",
    "tagName": "v${version}"
  },
  "npm": {
    "publish": false
  },
  "github": {
    "release": true,
    "releaseName": "${version}"
  }
}
